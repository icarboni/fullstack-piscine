version: '3.8'

services:
  nextjs:
    build: .
    ports:
      - "3000:3000"  # Expose port 3000
    volumes:
      - .:/app  # Bind mount the current directory to /app in the container (optional, useful for development)
    command: npm run start  # Override the default CMD in Dockerfile (optional)
